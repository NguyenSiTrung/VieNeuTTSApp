audiobook_epub_20260828 task B1: core/audiobook.py AudiobookLibrary(root). library.json index; <id>/book.json (chapters incl. text — self-contained shelf), state.json (statuses/errors/progress), ch_NNNN.wav atomic cache (part.wav+rename; soundfile infers format from suffix). id=sha256[:16] → moved-file re-import resumes. Ready-without-WAV reconciled to pending. Export 'NN - Title.wav' sanitized. 26 tests.
