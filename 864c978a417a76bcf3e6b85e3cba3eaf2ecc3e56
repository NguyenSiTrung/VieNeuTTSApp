phase0/task5: 3 offline strategies PASS (HF_HUB_OFFLINE user cache; portable HF_HOME copy - recommended; local backbone dir + onnx_dir, codec must stay in HF cache). Minimal CPU bundle = 16 files ~327MB (plan's 240MB estimate missed the 86MB codec repo). fp32 graphs live in onnx_update/ not onnx/. scripts/fetch_models.py: downloads minimal set + SHA256 manifest + --verify (tested 16/16 OK).
