audiobook_epub_20260828 tasks C1+C2: AppController synthesis-listener seam (attach/submit_stream_for_listener/detach; refuses while busy → no attach/complete race; routes progress/done/error incl. CANCELLED; shutdown detaches). PlaybackController +position/duration/seek (optional-signal duck contract). AudiobookController: shelf/chapters models, render-then-play, pipelined next-chapter pre-render, queued intents on busyChanged kick, auto-advance on finished (waits for in-flight render), resume seek from persisted progress, render-all, cancel (silent pending reset), export chapter/all. 47 new tests (10 seam + 37 controller); full suite 589 green.
